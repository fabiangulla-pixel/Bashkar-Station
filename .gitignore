# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/

# Builds (PyInstaller) — build/ pesa 252 MB
dist/
build/
*.spec.bak

# Datos y bases de datos (proyectos del investigador, nunca al repo)
*.db
*.sqlite
*.sqlite3
_prueba5/

# Modelos pesados (Kraken/CATMuS)
modelos/
*.mlmodel
*.traineddata

# Secretos y entorno
.env
*.key

# Marcador local de "dependencias ya instaladas" que usa Ejecutar.bat
.installed

# Imágenes de entrada del pase de OCR Vision a escala completa — viven como
# asset del release "vision-ocr-entrada-v1" (280MB, no en el historial de
# git). vision_ocr/salida/ SÍ se versiona: es el texto transcrito, el
# resultado real que vale la pena conservar.
vision_ocr/entrada/

# SO / editor
Thumbs.db
.DS_Store
desktop.ini
